import 'package:flutter/material.dart';

class InstitutionView extends StatelessWidget {
  const InstitutionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Institution Screen"));
  }
}
